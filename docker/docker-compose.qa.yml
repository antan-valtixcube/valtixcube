services:
  valtixcube_dev:
    image: valtixcube:qa            # ⭐ REQUIRED
    restart: always
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "3002:3000"
    command: ["npm", "start"]